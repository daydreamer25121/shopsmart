version: "3.8"
services:
  mongo:
    image: mongo:7
    container_name: shopsmart-mongo
    ports:
      - "27017:27017"
    volumes:
      - shopsmart-mongo-data:/data/db
    restart: unless-stopped
volumes:
  shopsmart-mongo-data:
